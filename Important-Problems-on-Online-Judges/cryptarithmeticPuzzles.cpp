#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    cin>>a>>b>>c;
    for(i=0;i<a.length();i++)
    {
        s.insert(a[i]);
    }
    for(i=0;i<b.length();i++)
    {
        s.insert(b[i]);
    }
    for(i=0;i<c.length();i++)
    {
        s.insert(c[i]);
    }
    vector<char> v(s.begin(),s.end(),)
    ans=solve()
    return 0;
}